extends layout

block contenido
  .contenido-principal
    h1 #{nombrePagina} - #{proyecto.nombre}

    if errores
      each error in errores
        .alerta.error= error.texto

    form.agregar-tarea(action=`/proyectos/${proyecto.url}` method="POST")
      .campo
        label(for="tarea") Tarea:
        input.nombre-tarea(type="text" name="tarea" id="tarea" placeholder="Nueva Tarea")
      .campo.enviar
        input.boton.nueva-tarea(type="submit" value="Guardar")

    .listado-pendientes
      if tareas.length
        ul
          each tarea in tareas
            li.tarea(data-tarea=tarea.id)
              p= tarea.tarea
              .acciones
                if tarea.estado
                  i.far.fa-check-circle.completo
                else
                  i.far.fa-check-circle

                i.fas.fa-trash
      else
        p No hay tareas en este proyecto.
        
    //- pre= vardump(tareas)

    // Avance del proyecto
    .avance
      h2 Avance del proyecto
      #barra-avance.barra-avance
        #porcentaje.porcentaje

    // Acciones que se pueden realizar
    .contenedor-acciones
      a(href=`/proyecto/editar/${proyecto.id}` class="boton") Editar Proyecto
      button#eliminar-proyecto(data-proyecto-url=proyecto.url type="button" class="boton eliminar") Eliminar Proyecto
